= simple_form_for([:admin, @company]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :content
  .form-inputs
    = f.input :avatar, as: :file

  .form-actions
    = f.button :submit
